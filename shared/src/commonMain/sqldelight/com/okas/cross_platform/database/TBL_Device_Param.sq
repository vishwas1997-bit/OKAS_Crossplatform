CREATE TABLE TBL_Device_Param (
  _id INTEGER PRIMARY KEY AUTOINCREMENT,
  configure_id TEXT NOT NULL,
  device_type TEXT NOT NULL,
  floor_id INTEGER NOT NULL,
  room_id INTEGER NOT NULL,
  zone2 INTEGER NOT NULL,
  device_id TEXT NOT NULL
);

insertDeviceParam:
INSERT OR REPLACE INTO TBL_Device_Param (
  _id,
  configure_id,
  device_type,
  floor_id,
  room_id,
  zone2,
  device_id
)
VALUES (
  ?,
  ?,
  ?,
  ?,
  ?,
  ?,
  ?
);